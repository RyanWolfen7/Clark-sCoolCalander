import React from 'react'
import { CalendarWrapper } from '../../ClarksComponents'

const Calander = (props) => {
    const { day, numDay, month, year } = props.date

    return (
        <CalendarWrapper>

        </CalendarWrapper>
    )
}

export default Calander 
